#include<iostream>
using namespace std;
//алгоритм сортування вибором
//int min_ellement(const int* arr, size_t size, size_t& offset)
//{
//    size_t offset = 0;
//	int min_el = arr[0];
//	for (int i = 0; i < size; i++)
//	{
//		if (arr[i] < min_el)
//		{
//			min_el = arr[i];
//			offset = i;
//		}
//	}
//	return min_el;
//}
//void sort(int* arr, size_t size)
//{
//	int temp;
//	size_t offset;
//	for (size_t i = 0; i < size-1; i++)
//	{
//		temp = min_ellement(arr + i, size, offset);
//		if (offset > 0)
//		{
//			arr[offset] = arr[i];
//			arr[i] = temp;
//		}
//	}
//}

